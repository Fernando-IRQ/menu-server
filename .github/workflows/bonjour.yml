name: Bonjour
on:
  - push
jobs:
  dire_bonjour:
    runs-on: ubuntu-22.04
    steps:
      - run: echo "Bonjour 👋"
      - uses: actions/checkout@v3
        with: 
            repository: workspace/menu-server
      - run: cat README.md # Affiche le contenu du fichier `README.md` à la base du dépôt
